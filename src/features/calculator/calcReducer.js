/*
const defaultState = {
    dislpaly: 0,
}

const calcReducer = (state, action) => {
    switch (action.type) {
        case "ADD":

        default:
            return state;
    }
}

export default calcReducer;
*/